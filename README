# hasm
This is an assembler for the hack assembly language, from the excellent [nand2tetris](http://nand2tetris.org/) course. The assembler is written in C.

## Build

`make`

## Usage

`./hasm <input file.asm>` compiles the input assembly file. Output file has the same name and directory as the input file.

## Links

* [nand2tetris](http://nand2tetris.org/) - the course homepage


